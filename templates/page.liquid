<div class="page-hero">
  <div class="wrap">
    <span class="eyebrow is-centered">{{ shop.name }}</span>
    <h1 class="h-xl">{{ page.title }}</h1>
  </div>
</div>

<section class="section">
  <div class="wrap">
    <div class="rte reveal">
      {{ page.content }}
    </div>
  </div>
</section>
