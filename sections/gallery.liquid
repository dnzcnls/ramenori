<section class="section gallery" id="gallery">
  <div class="wrap">
    <div class="section-head is-centered reveal">
      <span class="eyebrow is-centered">
        {{ section.settings.eyebrow }}
      </span>
      <h2 class="h-lg">
        {{ section.settings.heading }}
      </h2>
    </div>

    <div class="gallery-grid reveal">
      {%- for block in section.blocks -%}
        <div class="gallery-item{% if block.settings.size != blank %} {{ block.settings.size }}{% endif %}" {{ block.shopify_attributes }}>
          {%- if block.settings.image != blank -%}
            <img
              src="{{ block.settings.image | image_url }}"
              alt="{{ block.settings.alt | escape }}"
              loading="lazy"
            >
          {%- else -%}
            {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Gallery",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Photos"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "A Taste of the Table"
    }
  ],
  "blocks": [
    {
      "type": "photo",
      "name": "Photo",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "alt",
          "label": "Alt text",
          "default": "Gallery image"
        },
        {
          "type": "select",
          "id": "size",
          "label": "Size",
          "default": "",
          "options": [
            {
              "value": "",
              "label": "Normal"
            },
            {
              "value": "tall",
              "label": "Tall"
            },
            {
              "value": "wide",
              "label": "Wide"
            }
          ]
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Gallery",
      "blocks": [
        { "type": "photo" },
        { "type": "photo" },
        { "type": "photo" },
        { "type": "photo" },
        { "type": "photo" },
        { "type": "photo" }
      ]
    }
  ]
}
{% endschema %}